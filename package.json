{
  "name": "lesscode",
  "npmClient": "pnpm",
  "scripts": {
    "web:dev": "lerna exec --scope @lesscode/web npm run dev",
    "common:dev": "lerna exec --scope @lesscode/common npm run dev",
    "robot:start": "lerna exec --scope @lesscode/robot npm run start",
    "server:start": "lerna exec --scope @lesscode/server npm run start",
    "repl:dev": "lerna exec --scope @vue/repl npm run dev"
  },
  "devDependencies": {
    "lerna": "^8.1.6"
  }
}
